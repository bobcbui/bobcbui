# YouTube AI 漫剧导航页实施计划

## 项目目标

制作一个用于收集和浏览 YouTube AI 漫剧内容的单页导航站。视觉语言参考 YouTube 的清晰信息层级与视频卡片布局，但不复制其商标、Logo 或受保护素材。

页面实现集中在 `index.html`，分类、排名和作品信息全部由 `data.json` 配置。更新内容时原则上只需维护数据文件，不修改页面结构。

## 文件职责

### `data.json`

- 保存站点标题、简介和数据更新时间。
- 保存分类列表及其显示顺序。
- 保存作品、频道、排名和推荐状态。
- 不保存 HTML、CSS 或交互代码。
- 每个作品使用稳定且唯一的 `id`。

建议结构：

```json
{
  "site": {
    "title": "AI 漫剧导航",
    "description": "发现值得关注的 YouTube AI 漫剧",
    "updatedAt": "2026-09-15"
  },
  "categories": [
    { "id": "all", "name": "全部", "order": 0 }
  ],
  "items": [
    {
      "id": "example-id",
      "title": "作品名称",
      "channel": "频道名称",
      "url": "https://www.youtube.com/watch?v=...",
      "thumbnail": "https://...",
      "category": "fantasy",
      "rank": 1,
      "views": 0,
      "publishedAt": "2026-01-01",
      "featured": false,
      "description": "作品简介"
    }
  ]
}
```

### `index.html`

- 包含完整 HTML 结构、CSS 和 JavaScript。
- 加载、校验并展示 `data.json`。
- 实现搜索、分类、排名、排序、响应式和状态反馈。
- 不依赖框架或构建工具。

## 页面结构

### 1. Header

- 左侧显示站点名称和简洁图标。
- 中间放搜索框，可搜索作品名、频道名和简介。
- 右侧放数据更新时间或关于入口。
- 桌面端使用横向布局；移动端压缩为 Logo、搜索按钮和菜单按钮。

### 2. 分类导航

- 使用横向可滚动的分类标签，交互方式参考 YouTube 分类 Chips。
- 当前分类通过背景和文字颜色表示，不使用明显边框动画。
- 分类顺序由 `categories[].order` 控制。
- 切换分类时不刷新页面。

### 3. 热门排行榜

- 页面上部展示排名靠前的作品。
- 桌面端使用横向大卡片或紧凑榜单，移动端横向滚动。
- 显示排名、缩略图、作品名和频道名。
- 严格按有效的 `rank` 字段排序；无排名内容不进入榜单。

### 4. 作品网格

- 卡片结构为 16:9 缩略图、标题、频道、分类和播放数据。
- 桌面端显示 3～5 列，平板 2～3 列，手机 1～2 列。
- 点击卡片跳转至 YouTube，并设置安全的外链属性。
- 图片加载失败时显示统一占位图，避免布局塌陷。

### 5. 状态区域

- 首次加载显示骨架屏。
- 数据加载失败时显示错误说明和“重新加载”按钮。
- 搜索或分类无结果时显示空状态。
- 页面底部显示结果数量和数据更新时间。

## 交互逻辑

页面脚本按以下顺序组织：

1. 获取并校验 `data.json`。
2. 标准化缺省字段，过滤无效链接和重复 `id`。
3. 根据排名、分类和发布时间生成派生数据。
4. 响应搜索、分类切换和排序选择。
5. 使用 `DocumentFragment` 或 `template` 渲染，避免拼接不受信任的 HTML。
6. 将分类、关键词和排序方式同步到 URL 查询参数，刷新后可恢复。

搜索输入使用短延迟防抖。默认排序优先展示有效排名，其次按发布时间倒序；用户可切换“排行榜”“最新”和“最多播放”。

## 视觉规范

- 使用深灰文字、白色背景和浅灰分隔，保持内容导向。
- 红色仅用于当前状态、排名强调和主要按钮，不大面积铺色。
- 卡片默认不使用重阴影，悬停时只做轻微反馈。
- 缩略图统一使用 `aspect-ratio: 16 / 9` 和 `object-fit: cover`。
- 圆角控制在 8～12px，分类标签可使用胶囊形态。
- 动画控制在 120～180ms，并尊重 `prefers-reduced-motion`。

## 响应式与无障碍

- 重点验收宽度：360px、768px、1280px、1440px。
- 页面不得产生横向滚动，分类标签区域除外。
- 交互元素支持键盘操作，并提供清晰的 `:focus-visible` 状态。
- 搜索框提供可访问名称，分类按钮使用 `aria-pressed` 表示状态。
- 动态结果数量使用 `aria-live="polite"`。
- 图片提供替代文本，纯装饰图标使用 `aria-hidden="true"`。
- 点击区域不小于 44×44px。

## 数据校验规则

- `id`、`title`、`url`、`category` 为必填字段。
- `url` 只接受 `https://www.youtube.com/` 和 `https://youtu.be/`。
- `rank` 必须是大于 0 的整数，并检测重复名次。
- `views` 使用非负整数，展示时格式化为万或百万单位。
- `publishedAt` 使用 `YYYY-MM-DD` 格式。
- 分类不存在时归入“其他”，并在控制台输出数据警告。

## 实施顺序

1. 确定并填充 `data.json` 数据结构。
2. 在 `index.html` 建立语义化页面骨架和设计变量。
3. 完成数据加载、校验、搜索、分类和排序逻辑。
4. 实现排行榜、作品卡片、骨架屏、错误和空状态。
5. 完成桌面端与移动端响应式布局。
6. 补齐键盘操作、ARIA 属性和减少动画模式。
7. 使用真实数据测试链接、排名、图片和异常字段。

## 验收标准

- 页面只需同目录的 `data.json`，不依赖构建工具即可运行。
- 修改分类、排名或作品后，页面无需改代码即可更新。
- 搜索、分类和三种排序方式可以组合使用。
- 排行榜顺序与有效 `rank` 完全一致。
- YouTube 外链可正确打开，非法链接不会渲染为可点击入口。
- 加载中、加载失败和无结果状态均有明确反馈。
- 360px、768px、1280px 和 1440px 下布局完整且无内容遮挡。
- 可仅使用键盘完成搜索、分类、排序和打开作品。
- 控制台无未处理异常，数据问题有清晰警告。

## 范围限制

- 本计划只描述后续实现，不修改目标页面或数据文件。
- 不调用 YouTube API，不实现登录、订阅、评论或播放代理。
- 不在前端保存 API Key。
- 不复制 YouTube Logo、品牌图标或页面源码。
