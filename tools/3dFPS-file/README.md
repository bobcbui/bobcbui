# 3D FPS 资源目录

这个目录保留给 3D FPS 相关资源。

当前版本会加载本地 OBJ 模型：
- `model/enemy.obj`：方块风格敌人
- `model/knock.obj`：方块风格武器

场景本身仍保留少量程序化几何体和 Web Audio，方便后续继续扩展。

如果后续需要扩展，可以把资源放在这里，例如：
- `textures/`：贴图
- `models/`：glTF/GLB 模型
- `audio/`：枪声、击中特效、背景音乐